# Tuples:  thiis are immutable 
            # denoted by () 
            # repeated values are allowed.. also gives sorted elements..

# tpl = (1)  # it will be shown as integer type
# tpl = (1,)  # it will be shown as Tuple 

# tpl = tuple([10,])
# print(tpl)
# print(type(tpl))

tpl = (1,2,5,4,3)

l = sorted(tpl, reverse = True)

print(tpl)
print(tuple(l))

print()
